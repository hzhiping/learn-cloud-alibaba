server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.31.24:8088 #配置Nacos地址，集群模式
        #server-addr: localhost:8848    #配置Nacos地址，单机模式

management:
  endpoints:
    web:
      exposure:
        include: '*'